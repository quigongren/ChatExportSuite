# ChatExportSuite

Unified export toolkit for chat logs:
- PDF (print-engine based)
- HTML
- Markdown
- Text
- DOCX export via macOS helper pipelines

Local v3.2 development tree linked to Multiverse universe: chatexportsuite (U-CEEC-04X).

